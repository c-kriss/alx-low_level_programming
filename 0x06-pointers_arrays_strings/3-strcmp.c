#include <string.h>
#include "main.h"
/**
 * _strcmp - compare to strings s1 and s2
 * @s1: string input pointer
 * @s2: string input pointer
 * Return: strcmp(s1, s2)
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
