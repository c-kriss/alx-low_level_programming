#include <string.h>
#include "main.h"
/**
 * _strchr - return pointer on c occurence
 * @s: string input pointer
 * @c: string input variable
 * Return: s
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
